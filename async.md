<h1 align="center">Asynchronous</h1>

You can write a maintainable async codes with typescript/javascript `async await` support.

```typescript
class HomeService {

  public async findJob() {
    const result = await somethingAsync();
  }
}
```
